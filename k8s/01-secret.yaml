# Les valeurs sont ecrites en clair ici pour la correction du projet.
# En production ce fichier n'est pas versionne : il est genere par
# `kubectl create secret generic todo-db --from-env-file=.env`
apiVersion: v1
kind: Secret
metadata:
  name: todo-db
  namespace: todo
type: Opaque
stringData:
  POSTGRES_DB: todo
  POSTGRES_USER: todo
  POSTGRES_PASSWORD: Ch4ngeM01!
